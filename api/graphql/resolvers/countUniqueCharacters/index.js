const { cleanAndGetUpperCaseText } = require("./cleanAndGetUpperCaseText");
const countUniqueCharacters = require("./countUniqueCharacters");
const { isInputValid } = require("./isInputValid");

module.exports = {
  cleanAndGetUpperCaseText,
  countUniqueCharacters,
  isInputValid,
}
